<?xml version="1.0" encoding="UTF-8"?>
<!--
  Licensed to the Apache Software Foundation (ASF) under one
  or more contributor license agreements.  See the NOTICE file
  distributed with this work for additional information
  regarding copyright ownership.  The ASF licenses this file
  to you under the Apache License, Version 2.0 (the
  "License"); you may not use this file except in compliance
  with the License.  You may obtain a copy of the License at

      https://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing,
  software distributed under the License is distributed on an
  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  KIND, either express or implied.  See the License for the
  specific language governing permissions and limitations
  under the License.
  -->
<project xmlns="http://maven.apache.org/POM/4.1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 http://maven.apache.org/xsd/maven-4.1.0.xsd">


  <parent>
    <groupId>org.apache.plc4x</groupId>
    <artifactId>plc4j-tools</artifactId>
    <version>1.0.0.B-SNAPSHOT</version>
  </parent>

  <artifactId>plc4j-plc4x-server</artifactId>

  <name>PLC4J: Tools: Plc4x Server</name>
  <description>Server that can be used by drivers of the 'plc4x' protocol to relay requests which are then executed on this machine.</description>

  <properties>
    <project.build.outputTimestamp>2024-02-16T14:53:02Z</project.build.outputTimestamp>
  </properties>

  <build>
    <plugins>
      <!-- Build a fat jar containing all dependencies -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <id>generate-uber-jar</id>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers combine.children="append">
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>org.apache.plc4x.java.tools.plc4xserver.Plc4xServer</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-uber-jar</id>
            <phase>package</phase>
            <goals>
              <goal>attach-artifact</goal>
            </goals>
            <configuration>
              <artifacts>
                <artifact>
                  <file>${project.build.directory}/${project.artifactId}-${project.version}-uber-jar.jar</file>
                  <type>jar</type>
                  <classifier>standalone</classifier>
                </artifact>
              </artifacts>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <configuration>
          <usedDependencies combine.children="append">
            <usedDependency>org.slf4j:log4j-over-slf4j</usedDependency>
            <usedDependency>org.apache.plc4x:plc4j-driver-s7</usedDependency>
            <usedDependency>org.apache.plc4x:plc4j-driver-simulated</usedDependency>
          </usedDependencies>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>report</id>
            <configuration>
              <excludes combine.children="append">
                <!-- Server lifecycle, main(), shutdown hooks — not unit-testable -->
                <exclude>org/apache/plc4x/java/tools/plc4xserver/Plc4xServer*.class</exclude>
                <!-- Complex protocol adapter with read/write/connect dispatch — requires integration tests -->
                <exclude>org/apache/plc4x/java/tools/plc4xserver/protocol/Plc4xServerAdapter.class</exclude>
              </excludes>
            </configuration>
          </execution>
          <execution>
            <id>check-coverage</id>
            <configuration>
              <excludes combine.children="append">
                <exclude>org/apache/plc4x/java/tools/plc4xserver/Plc4xServer*.class</exclude>
                <exclude>org/apache/plc4x/java/tools/plc4xserver/protocol/Plc4xServerAdapter.class</exclude>
              </excludes>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- PLC4X dependencies -->
    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-api</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
    </dependency>
    <!-- SPI sub-modules (plc4j-spi is now pom-only after SPI restructuring) -->
    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-spi-drivers</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-spi-buffers-byte</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-spi-values</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
    </dependency>

    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-tools-connection-cache</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
    </dependency>

    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-driver-plc4x</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
    </dependency>

    <!-- Include only needed drivers (plc4j-driver-all includes drivers not yet
         adapted to the new SPI, causing ServiceLoader conflicts) -->
    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-driver-s7</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.plc4x</groupId>
      <artifactId>plc4j-driver-simulated</artifactId>
      <version>1.0.0.B-SNAPSHOT</version>
      <scope>runtime</scope>
    </dependency>

    <!-- bit-io: needed by plc4j-spi-buffers-byte but missing from its pom after SPI split -->
    <dependency>
      <groupId>com.github.jinahya</groupId>
      <artifactId>bit-io</artifactId>
      <version>${bit-io.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>io.netty</groupId>
      <artifactId>netty-buffer</artifactId>
    </dependency>
    <dependency>
      <groupId>io.netty</groupId>
      <artifactId>netty-transport</artifactId>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>log4j-over-slf4j</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <!-- Explicitly override the scope to compile to include these -->
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-core</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

</project>
